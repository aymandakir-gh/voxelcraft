{
  "name": "voxelcraft",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based voxel sandbox game — original code and procedural textures, inspired by the block-building genre.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "license": "MIT"
}
